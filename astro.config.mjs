// @ts-check
import { defineConfig } from 'astro/config';

const base = '/shopify-theme-store-checklist';

export default defineConfig({
  site: 'https://saabbir.github.io',
  base,
  trailingSlash: 'always',
});
